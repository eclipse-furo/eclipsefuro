## Notes
The files in `./proto` and `./proto_dependencies` are sample files to generate the schema files. They are used to test the plugin and to generate the schema files.

The generated schema files are stored without a directory structure (flat) in `./open-models/schema` without any file extension.