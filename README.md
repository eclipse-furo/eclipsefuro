## フロー Furo

Since we moved from the interpreted specs in @furo/data to the generated types and typescript code in @furo/open-models , 
we decided to get rid of our own specification format and use protobuf as the only Interface Definition Language (IDL).

The documentation is still work in progress.
